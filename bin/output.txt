#include <stdio.h>

void main() {
   Int i;
   Int soma;
   Int somaFor;
   Int n;
   Int verd;

   n = 100;
   soma = 0;
   i = 0;
   verd = 2;
   while ((i < n) && !!verd) {
      if ( ((i % 2) == (0 + ((0 * 0) / 1))) ) { 
         soma = (--soma + (i * i));
      }
      i = (i + 1);
   }
   somaFor = 0;
   for (int k; k = 0; k <= 100) {
      if ( ((i % 2) == 0) ) { 
         somaFor = (somaFor + (k * k));
      }
   }
   printf("%d\n", soma);
   printf("%d\n", somaFor);
}
